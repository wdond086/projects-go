{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Task Manager CLI",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}/task-cli",
            "mode": "debug"
        }
    ]
}